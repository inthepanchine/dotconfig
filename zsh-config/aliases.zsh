#
# Aliases file.
#

# Alias ls to exa
alias ls="exa -l --icons --group-directories-first --git --sort=Name  --ignore-glob=\".DS_Store\""

# Alias for polishcow (try it :))
alias polish-cow="${POLISHCOW_PATH}"

# Alias for luamake
alias luamake="${HOME}/.cache/nvim/lspconfig/sumneko_lua/lua-language-server/3rd/luamake/luamake"
